# core-JBIG2

Pure-Python JBIG2 parsing and decoding components.

PDF integrations can use `assemble_embedded_jbig2` for PDF-embedded streams;
PDF filter parameters and error translation remain the responsibility of the
integration layer.
