[project]
name = "core-font-programs"
version = "0.0.1"
description = "Generic TrueType and CFF font-program components"
requires-python = ">=3.11"
license = "AGPL-3.0-only"
license-files = ["LICENSE.txt"]
dependencies = ["fonttools>=4.63.0,<5.0.0"]

[build-system]
requires = ["uv_build>=0.11.17,<0.12.0"]
build-backend = "uv_build"
