# core-CCITT

Pure-Python CCITT Group 3/4 fax decoding components.

The package is format-focused and does not depend on PDF objects or PDF
filter semantics. PDF integrations can translate their own parameters into
`decode_ccitt_fax` arguments.
